[Service]
# Install as /etc/systemd/system/hearthbit-relay.service.d/network.conf when
# LAN, MQTT or Matrix is enabled. The base unit intentionally permits only
# D-Bus and Bluetooth sockets.
RestrictAddressFamilies=AF_UNIX AF_BLUETOOTH AF_INET AF_INET6
